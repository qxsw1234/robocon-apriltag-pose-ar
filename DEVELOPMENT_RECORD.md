# AprilTag 位姿估计与 AR 开发记录

## 1. 任务拆解

普通题需要完成摄像头图像发布、AprilTag 检测、ID/四角点/中心标注、相机标定、PnP 三维位姿、TF、图像坐标轴、RViz2 显示及真实距离误差验证。拓展题要求虚拟三维物体在 AprilTag 世界坐标系中保持固定，并针对稳定性问题进行改进。

## 2. 已完成开发

- ROS 2 Python 包、launch、配置文件和 RViz2 配置；
- `v4l2_camera` 图像输入和 CameraInfo 接口；
- AprilTag 检测结果可视化；
- `solvePnP`、`rvec/tvec`、TF 与三维坐标轴；
- 距离采集节点、CSV 记录和误差报告脚本；
- AR 立方体/三棱锥/箭头投影及 RViz Marker；
- 平移滤波、四元数符号对齐、PnP 候选连续性选择、异常帧剔除和丢检保持；
- 8 个离线视角的 AR 闭环验证；
- 最终 82/82 单元测试通过，2026-08-28 重跑的构建、测试与汇总日志保存在 `docs/test_logs/`。

## 3. 调试与验证过程

- 摄像头节点编号随插拔变化：改用 `/dev/v4l/by-id/` 稳定路径；
- RGB 输出帧率低：使用 `mono8` 将实测输入帧率由约 8 Hz 提升到约 30 Hz；
- 平面 PnP 可能发生姿态翻转：枚举候选解并结合时序连续性择优；
- 四元数直接平均会因 `q` 与 `-q` 等价而抵消：平均前统一符号；
- AR 物体不能只贴在标签表面：在标签坐标系中设置 y/z 三维偏移，并用多视角闭环验证其世界系位置不漂移。

详细稳定性实验和数值见 `src/apriltag_pose/STABILITY.md`。

## 4. 尚未完成及原因

当前 `src/apriltag_pose/config/camera.yaml` 是零内参占位文件。本次提交未打印棋盘格/标签并完成真实摄像头标定，因此未产生真实距离 CSV、误差统计报告或实机 AR 视频。提交时保留完整采集与分析代码、离线验证图和明确的后续操作步骤，不将合成结果当作实机实验。

后续工作：打印标定物，标定当前摄像头，采集 0.30/0.50/0.70/1.00 m 正对与倾斜数据，生成 MAE/RMSE 报告，并录制移动相机时 AR 物体保持世界系固定的视频。

## 5. AI 工具使用说明

本项目在环境配置、代码调试、单元测试设计、稳定性方案分析和文档整理过程中使用了 Codex、ZCode/Claude 等 AI 编程工具辅助。ROS 2 数据流、PnP/TF 变换、滤波方法和 AR 投影关系均由本人结合代码与测试结果理解、检查并整理。AI 未替代真实硬件实验；未完成的实机部分已明确标注。
